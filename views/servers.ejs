<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select Server - CodeFauzanBot</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #0f0f1e 0%, #1a1a2e 100%);
            color: #fff;
            min-height: 100vh;
            padding: 40px 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 48px;
            position: relative;
            z-index: 1;
        }

        .header h1 {
            font-size: 36px;
            font-weight: 800;
            margin-bottom: 12px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .header p { color: #b9bbbe; font-size: 16px; }

        .logout-btn {
            position: fixed;
            top: 24px;
            right: 24px;
            padding: 12px 24px;
            background: rgba(26, 26, 46, 0.8);
            color: #fff;
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .logout-btn:hover {
            background: #f04747;
            border-color: #f04747;
        }

        .server-grid {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 24px;
            position: relative;
            z-index: 1;
        }

        .server-card {
            background: rgba(26, 26, 46, 0.8);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 16px;
            padding: 24px;
            text-align: center;
            transition: all 0.3s;
        }

        .server-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 60px rgba(0,0,0,0.6);
            border-color: #5865f2;
        }

        .server-icon {
            width: 96px;
            height: 96px;
            border-radius: 50%;
            margin: 0 auto 16px;
            border: 4px solid #5865f2;
            box-shadow: 0 8px 24px rgba(88,101,242,0.3);
            object-fit: cover;
        }

        .server-name {
            font-size: 18px;
            font-weight: 700;
            margin-bottom: 8px;
        }

        .server-role {
            font-size: 14px;
            color: #72767d;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
        }

        .server-btn {
            width: 100%;
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            display: inline-block;
        }

        .btn-manage {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .btn-manage:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(88,101,242,0.6);
        }

        .btn-setup {
            background: #43b581;
            color: white;
        }

        .btn-setup:hover {
            background: #3aa76d;
            transform: translateY(-2px);
        }

        @media (max-width: 768px) {
            .header h1 { font-size: 28px; }
            .server-grid { grid-template-columns: 1fr; }
            .logout-btn { position: static; width: 100%; margin-bottom: 24px; justify-content: center; }
        }
    </style>
</head>
<body>
    <a href="/logout" class="logout-btn">
        <i class="fas fa-sign-out-alt"></i> Logout
    </a>

    <div class="header">
        <h1><i class="fas fa-server"></i> Select Your Server</h1>
        <p>Choose a server to manage with CodeFauzanBot</p>
    </div>

    <div class="server-grid">
        <% guilds.forEach(guild => { %>
            <div class="server-card">
                <img src="<%= guild.icon ? `https://cdn.discordapp.com/icons/${guild.id}/${guild.icon}.png` : 'https://via.placeholder.com/96' %>" 
                     alt="<%= guild.name %>" 
                     class="server-icon">
                <div class="server-name"><%= guild.name %></div>
                <div class="server-role">
                    <i class="fas fa-<%= guild.owner ? 'crown' : 'shield-alt' %>"></i>
                    <%= guild.owner ? 'Owner' : 'Administrator' %>
                </div>
                <% if (guild.hasBot) { %>
                    <a href="/dashboard/<%= guild.id %>" class="server-btn btn-manage">
                        <i class="fas fa-cog"></i> Manage Server
                    </a>
                <% } else { %>
                    <a href="<%= botInviteLink %>&guild_id=<%= guild.id %>" class="server-btn btn-setup">
                        <i class="fas fa-plus"></i> Add Bot & Setup
                    </a>
                <% } %>
            </div>
        <% }); %>
    </div>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CodeFauzanBot - Login</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #0f0f1e 0%, #1a1a2e 100%);
            color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            position: relative;
            overflow: hidden;
        }

        body::before {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 50%, rgba(88, 101, 242, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(114, 137, 218, 0.1) 0%, transparent 50%);
            animation: pulse 15s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }

        .container {
            position: relative;
            z-index: 1;
            text-align: center;
            background: rgba(26, 26, 46, 0.8);
            backdrop-filter: blur(20px);
            padding: 60px 50px;
            border-radius: 24px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.1);
            max-width: 450px;
            width: 90%;
        }

        .logo {
            width: 100px;
            height: 100px;
            border-radius: 20px;
            margin: 0 auto 30px;
            border: 3px solid #5865f2;
            box-shadow: 0 8px 32px rgba(88, 101, 242, 0.4);
        }

        h1 {
            font-size: 36px;
            font-weight: 800;
            margin-bottom: 16px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        p {
            font-size: 16px;
            color: #b9bbbe;
            margin-bottom: 40px;
            line-height: 1.6;
        }

        .btn-discord {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            padding: 16px 32px;
            background: #5865f2;
            color: #ffffff;
            text-decoration: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 700;
            transition: all 0.3s ease;
            box-shadow: 0 8px 24px rgba(88, 101, 242, 0.4);
        }

        .btn-discord:hover {
            background: #4752c4;
            transform: translateY(-2px);
            box-shadow: 0 12px 32px rgba(88, 101, 242, 0.6);
        }

        .btn-discord i {
            font-size: 24px;
        }

        .features {
            margin-top: 50px;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
        }

        .feature {
            padding: 16px;
            background: rgba(47, 49, 54, 0.5);
            border-radius: 12px;
            transition: all 0.3s ease;
        }

        .feature:hover {
            background: rgba(88, 101, 242, 0.1);
            transform: translateY(-4px);
        }

        .feature i {
            font-size: 28px;
            margin-bottom: 8px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .feature p {
            font-size: 12px;
            margin: 0;
            color: #72767d;
            font-weight: 600;
        }

        @media (max-width: 768px) {
            .container {
                padding: 40px 30px;
            }

            h1 {
                font-size: 28px;
            }

            .features {
                grid-template-columns: 1fr;
                gap: 12px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <img src="https://iili.io/3SRUsTP.md.png" alt="CodeFauzan Bot" class="logo">
        <h1>CodeFauzanBot</h1>
        <p>Powerful Discord bot with advanced features.<br>Manage your server with ease!</p>
        
        <a href="/auth/discord" class="btn-discord">
            <i class="fab fa-discord"></i>
            Login with Discord
        </a>

        <div class="features">
            <div class="feature">
                <i class="fas fa-trophy"></i>
                <p>Leveling System</p>
            </div>
            <div class="feature">
                <i class="fas fa-shield-alt"></i>
                <p>Auto Moderation</p>
            </div>
            <div class="feature">
                <i class="fas fa-chart-line"></i>
                <p>Analytics</p>
            </div>
        </div>
    </div>
</body>
</html>